{
  "analysis": {
    "tokenizer": {
      "nori_user_tokenizer": {
        "type": "nori_tokenizer",
        "decompound_mode": "mixed"
      }
    },
    "analyzer": {
      "nori_analyzer": {
        "type": "custom",
        "tokenizer": "nori_user_tokenizer",
        "filter": ["lowercase", "nori_part_of_speech"]
      }
    }
  }
}
